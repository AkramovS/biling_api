# Biling API

REST API для управления тарифами с JWT-авторизацией и RBAC (Role-Based Access Control).

Реализовано по паттернам **Alex Edwards** из "Let's Go Further".

## ✨ Возможности

- 🔐 **JWT аутентификация** с HMAC-SHA256
- 👥 **RBAC** - управление доступом на основе групп и разрешений (FID)
- 🔄 **Оптимистичная блокировка** для предотвращения конфликтов при обновлении
- 🛡️ **Bcrypt** хеширование паролей (cost 12)
- 🚀 **Graceful shutdown** сервера
- ⚡ **Middleware** для аутентификации и авторизации
- 📊 **Тестовые данные** для быстрого старта

## 📋 Требования

- **Go 1.21+**
- **PostgreSQL 14+**

## 🚀 Быстрый старт

### Вариант A: Автоматическая установка (рекомендуется)

```powershell
# Windows PowerShell
.\init.ps1
```

Скрипт автоматически:

- ✅ Проверит PostgreSQL и Go
- ✅ Создаст базу данных
- ✅ Применит миграции
- ✅ Создаст администратора с правами
- ✅ Настроит .env файл

### Вариант B: Ручная установка

#### 1. Создать базу данных

```bash
createdb biling_db
```

#### 2. Применить миграции

```bash
psql -U postgres -d biling_db -f migrations/000001_create_tables.up.sql
psql -U postgres -d biling_db -f migrations/000002_seed_data.up.sql
```

#### 3. Создать системного пользователя

```bash
# Генерировать хеш пароля
cd tools
go run hash_password.go password123

# Применить SQL (см. TESTING.md)
```

#### 4. Создать `.env` файл

```env
PORT=4000
ENV=development
DB_DSN=postgres://postgres:postgres@localhost/biling_db?sslmode=disable
JWT_SECRET=your-super-secret-jwt-key-change-in-production
```

#### 5. Запустить сервер

```bash
go run ./cmd/api
```

Сервер запустится на `http://localhost:4000`

---

📖 **Полная инструкция:** [TESTING.md](TESTING.md) | 🚀 **Шпаргалка:** [CHEATSHEET.md](CHEATSHEET.md)

## 🌐 API Endpoints

### Публичные эндпоинты

- `GET /v1/health` - Проверка состояния API
- `POST /v1/auth/login` - Вход (получение JWT токена)

### Защищенные эндпоинты (требуют JWT токен)

- `GET /v1/users/:id/accounts` - Получить аккаунты пользователя

  - Требуется право: **FIDAccountsRead (1)**
- `GET /v1/account-tariffs/:id` - Получить информацию о тарифе аккаунта

  - Требуется право: **FIDTariffsRead (2)**
- `PATCH /v1/account-tariffs/:id` - Изменить тариф аккаунта

  - Требуется право: **FIDTariffsUpdate (3)**
  - Использует оптимистичную блокировку (версионирование)

## 📝 Примеры использования

### 1. Проверка состояния API

```bash
curl http://localhost:4000/v1/health
```

### 2. Вход в систему

```bash
curl -X POST http://localhost:4000/v1/auth/login ^
  -H "Content-Type: application/json" ^
  -d "{\"login\":\"admin\",\"password\":\"password123\"}"
```

**Ответ:**

```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "login": "admin",
    "created_at": "2024-01-01T00:00:00Z"
  }
}
```

### 3. Получение аккаунтов пользователя

```bash
curl -H "Authorization: Bearer YOUR_TOKEN" ^
  http://localhost:4000/v1/users/1/accounts
```

**Успешный ответ:**

```json
{
  "user": {
    "id": 1,
    "name": "Иван Петров"
  },
  "accounts": [
    {"id": 1},
    {"id": 2}
  ]
}
```

**Без прав (403 Forbidden):**

```json
{
  "error": "your user account doesn't have the necessary permissions to access this resource"
}
```

### 4. Изменение тарифа (с оптимистичной блокировкой)

```bash
curl -X PATCH http://localhost:4000/v1/account-tariffs/1 ^
  -H "Authorization: Bearer YOUR_TOKEN" ^
  -H "Content-Type: application/json" ^
  -d "{\"tariff_id\":2,\"version\":1}"
```

**Успешный ответ:**

```json
{
  "account_tariff": {
    "id": 1,
    "account_id": 1,
    "tariff_id": 2,
    "version": 2,
    "updated_at": "2024-01-01T12:34:56Z",
    "updated_by": 1,
    "updated_by_user": {
      "id": 1,
      "login": "admin"
    }
  }
}
```

**Конфликт версий (409 Conflict):**

```json
{
  "error": {
    "code": "version_conflict",
    "message": "Record was modified by another user..."
  },
  "server": { "data": {...}, "meta": {...} },
  "client": { "data": {...}, "meta": {...} }
}
```

## 🧪 Тестовые данные

После применения миграций автоматически создаются:

### Бизнес-пользователи (users)

- **5 пользователей:** Иван Петров, Мария Сидорова, Алексей Козлов, Елена Новикова, Дмитрий Волков
- **3 аккаунта** (accounts)
- **7 связей** пользователей с аккаунтами (users_accounts)

### Тарифы (tariffs)

- **Тариф 1** - 100.00 см.
- **Тариф 2** - 200.00 см.
- **Тариф 3** - 300.00 см.

### Создание системного пользователя

Для работы с API создайте пользователя с правами:

```sql
-- Создать админа
INSERT INTO system_accounts (login, password, name) 
VALUES ('admin', '$2a$12$...хеш_bcrypt...', 'Администратор');

-- Создать группу
INSERT INTO system_group_info (name, description) 
VALUES ('Администраторы', 'Полный доступ');

-- Назначить права (FID 1, 2, 3)
INSERT INTO system_rights (group_id, fid) VALUES (1, 1), (1, 2), (1, 3);

-- Добавить в группу
INSERT INTO system_groups (group_id, user_id) VALUES (1, 1);
```

**Или используйте:** `tools/hash_password.go` для генерации хеша пароля.

## Структура проекта

```
.
├── cmd/
│   └── api/              # Главное приложение
│       ├── main.go       # Точка входа
│       ├── routes.go     # Роуты
│       ├── middleware.go # Аутентификация/авторизация
│       ├── helpers.go    # Вспомогательные функции
│       ├── errors.go     # Обработка ошибок
│       └── *_handlers.go # Обработчики запросов
├── internal/
│   ├── data/            # Модели данных
│   │   ├── models.go
│   │   ├── users.go
│   │   ├── accounts.go
│   │   ├── auth_users.go
│   │   ├── groups.go
│   │   └── tokens.go
│   └── validator/       # Валидация
│       └── validator.go
├── migrations/          # SQL миграции
└── go.mod
```

## Конфигурация

Переменные окружения и флаги:

```bash
go run ./cmd/api \
  -port=4000 \
  -env=development \
  -db-dsn="postgres://postgres:postgres@localhost/utm?sslmode=disable" \
  -jwt-secret="your-secret-key-change-this-in-production"
```

## 🔐 RBAC (Role-Based Access Control)

Система прав доступа основана на **FID (Feature ID)**:

### Архитектура

```
system_accounts (пользователи)
    ↓
system_groups (членство в группах)
    ↓
system_group_info (информация о группах)
    ↓
system_rights (права по FID)
```

### Права доступа (FID)

Определены в `internal/data/permissions.go`:

- **FIDAccountsRead (1)** - Чтение аккаунтов пользователей
- **FIDTariffsRead (2)** - Чтение информации о тарифах
- **FIDTariffsUpdate (3)** - Изменение тарифов

### Как это работает

1. Пользователь входит → получает JWT токен
2. При запросе токен проверяется через middleware
3. Проверяется наличие требуемого FID через цепочку таблиц
4. Если права нет → 403 Forbidden

**Подробнее:** [TARIFF_ACCESS_EXPLAINED.md](TARIFF_ACCESS_EXPLAINED.md)

## 🛡️ Безопасность

- ✅ **Bcrypt** - Пароли хешируются с cost 12
- ✅ **JWT HMAC-SHA256** - Токены подписаны секретным ключом
- ✅ **24 часа** - Время жизни токена
- ✅ **Middleware** - Проверка токена и прав на каждый запрос
- ✅ **Оптимистичная блокировка** - Предотвращение конфликтов обновления
- ✅ **SQL injection** - Защита через параметризованные запросы
- ✅ **Context timeout** - Защита от долгих запросов к БД (3 сек)

## 📚 Документация

- 📖 [TESTING.md](TESTING.md) - Полная инструкция по тестированию API
- 🚀 [CHEATSHEET.md](CHEATSHEET.md) - Шпаргалка с быстрыми командами
- 📊 [SQL_QUERIES.md](SQL_QUERIES.md) - SQL запросы для администрирования
- 🔍 [TARIFF_ACCESS_EXPLAINED.md](TARIFF_ACCESS_EXPLAINED.md) - Подробно о системе прав
- 📝 [QUICKSTART.md](QUICKSTART.md) - Быстрый старт (альтернативная инструкция)

## 🛠️ Утилиты

- 🔐 `tools/hash_password.go` - Генератор bcrypt хешей паролей
- 🚀 `init.ps1` - Автоматическая инициализация проекта (Windows)

## 🤝 Вклад

Проект следует паттернам и best practices из книги **"Let's Go Further"** by Alex Edwards.

### Архитектура

- **Clean Architecture** - Разделение на слои (handlers → models → database)
- **Middleware Chain** - Модульная система middleware
- **Context-based Auth** - Пользователь в контексте запроса
- **Optimistic Locking** - Версионирование для предотвращения конфликтов
- **Envelope Pattern** - Обертка для JSON ответов

## 📄 Лицензия

MIT License
